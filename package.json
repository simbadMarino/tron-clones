{
  "name": "tron-create2",
  "version": "0.0.1",
  "scripts": {
    "compile": "tronbox compile",
    "deploy:shasta": "tronbox migrate --network shasta",
    "deploy:nile": "tronbox migrate --network nile",
    "deploy:mainnet": "tronbox migrate --network nile",
    "test:shasta": "tronbox test --network shasta",
    "test:nile": "tronbox test --network nile",
    "prepare": "husky"
  },
  "license": "MIT",
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0",
    "bs58": "^6.0.0",
    "dotenv": "^16.0.0",
    "js-sha3": "^0.9.3",
    "tronweb": "^6.0.4"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7"
  }
}
